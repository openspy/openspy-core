qr:

v1 & v2 protocol tests
add server
remove server
delete peer & try connection with same instance key/ip+port combo
test recieve message
test available queries

LATER:
add python script + test key override on per game basis


sb:
v1 tests
